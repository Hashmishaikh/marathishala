{
  "name": "msca",
  "version": "1.0.0",
  "description": "MarathiShala Cricket Association (MSCA) Full-Stack Application",
  "license": "ISC",
  "author": "Dungeon Master",
  "type": "commonjs",
  "main": "backend/server.js",
  "scripts": {
    "start": "node ./backend/server.js",
    "server": "node ./backend/server.js",
    "dev": "nodemon ./backend/server.js",
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "render-build": "npm install && npm --prefix frontend install && npm --prefix frontend run build",
    "seed": "node ./backend/seed.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.9.4",
    "morgan": "^1.12.0",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}